{
  "name": "flancommand",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "docker compose up --build",
    "format": "prettier --write package.json pnpm-workspace.yaml tsconfig.base.json tsconfig.e2e.json vitest.config.ts playwright.config.ts README.md eslint.config.mjs .prettierrc.json .github/workflows/check.yml packages/config apps probe tests/e2e docs/operations/local-development.md docs/decisions docs/protocol docs/phase-reports CONTRIBUTING.md compose.yaml",
    "format:check": "prettier --check package.json pnpm-workspace.yaml tsconfig.base.json tsconfig.e2e.json vitest.config.ts playwright.config.ts README.md eslint.config.mjs .prettierrc.json .github/workflows/check.yml packages/config apps probe tests/e2e docs/operations/local-development.md docs/decisions docs/protocol docs/phase-reports CONTRIBUTING.md compose.yaml",
    "typecheck:e2e": "tsc -p tsconfig.e2e.json --noEmit",
    "test:e2e": "npm run build && npm run typecheck:e2e && playwright test --project=chromium",
    "test:e2e:firefox": "npm run build && npm run typecheck:e2e && playwright test --project=firefox",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.base.json && npm run build --workspace @flancommand/event-schema && npm run build --workspace @flancommand/hermes-adapter",
    "storage:backup": "node dist/apps/api/src/storage-backup-cli.js",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run typecheck:e2e && npm test && npm run build"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "probe/*"
  ],
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "@playwright/test": "1.61.1",
    "@types/node": "^24.0.3",
    "eslint": "^9.30.1",
    "eslint-config-prettier": "^10.1.5",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.1",
    "vitest": "^3.2.4"
  }
}
