# Protocol fixtures

Fixtures are sanitized, bounded examples of Hermes frames. They must contain no credentials, private message content, or raw secrets. `mock-stream.jsonl` is deterministic and safe for tests. Live transcripts belong in ignored probe output and may be copied here only after review.
