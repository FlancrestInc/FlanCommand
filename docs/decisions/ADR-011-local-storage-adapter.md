# ADR-011: Start with a local storage adapter

Status: Provisional.

Use a storage interface with local filesystem backing for development and early deployment. Keep the interface ready for object storage later. Enforce path containment and safe filenames.
